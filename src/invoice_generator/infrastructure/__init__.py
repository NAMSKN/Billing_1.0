"""Infrastructure layer: SQLite persistence, PDF, printing, backup, filesystem.

Adapters that implement domain repository ports and other outward-facing
concerns. Infrastructure depends on the domain, never the reverse.
"""
