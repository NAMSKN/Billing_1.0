"""Exploratory spikes (not part of the shipped application package).

Spikes prove out risky integrations early (e.g. ReportLab PDF generation,
Windows preview/printing) before the production implementation is built. They
are intentionally minimal and are not wired into the application.
"""
